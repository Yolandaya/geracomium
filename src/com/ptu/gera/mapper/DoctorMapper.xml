<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ptu.gera.mapper.DoctorMapper">  <!-- 要映射的mapper的完整包+mapper名称-->
<select id="queryAllDoctor" resultType="doctor">
	select * from doctor
</select>
	<insert id="infodoctor">
		insert into reservedoctor(duser_id,ddoctor_id) value (#{0},#{1})
	</insert>
</mapper>

